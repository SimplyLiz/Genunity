# GPU-enabled CellForge image (stub)
FROM nvidia/cuda:12.4.0-runtime-ubuntu22.04

# TODO: Install Python, Rust, build cellforge with GPU support
RUN apt-get update && apt-get install -y python3 python3-pip && rm -rf /var/lib/apt/lists/*

WORKDIR /app
EXPOSE 8420

CMD ["echo", "GPU Dockerfile is a stub — build not yet implemented"]

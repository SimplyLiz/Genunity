{
  "build": {
    "dockerfilePath": "docker/Dockerfile"
  },
  "deploy": {
    "startCommand": "alembic upgrade head && uvicorn cellforge.api.app:create_app --factory --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/v1/health"
  }
}
